package edu.kit.rose.model.roadsystem.elements;

/**
 * An enum describing the different kinds of Connectors. Used to differentiate between connectors of a
 * {@link edu.kit.rose.model.roadsystem.elements.Segment}.
 */
public enum ConnectorType {
  ENTRY,
  EXIT,
  RAMP_ENTRY,
  RAMP_EXIT
}
